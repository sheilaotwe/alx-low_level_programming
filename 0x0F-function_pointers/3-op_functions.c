#include "3-calc.h"

/**
 * op_add - Function that adds two integers a and b
 * @a: interger one
 * @b: interger two
 * Return: sum, a + b
 */
int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - Function that subtracts two integers a and b
 * @a: integer one
 * @b: integer two
 * Return: difference, a - b
 */
int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - Function that multiples two integers a and b
 * @a: integer one
 * @b: integer two
 * Return: product, a * b
 */
int op_mul(int a, int b)
{
	return (a * b)
}

/**
 * op_div - Function that divides two integers a and b
 * @a: integer one
 * @b: integer two
 * Return: quotient, a / b
 */
int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod - Function that mods two integers a and b
 * @a: integer one
 * @b: integer two
 * Return: mod, a % b
 */
int op_mod(int a, int b)
{
	return (a % b);
}
